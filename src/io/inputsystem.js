//TODO better name?

import ENSURE from '../utils/ensure';

let InputSystem = function (settings) {
  ENSURE(settings);

  let system = {
    //TODO
  };
  return system;
};

export default InputSystem;